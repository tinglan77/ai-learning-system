<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ailearning.mapper.ChapterMapper">

    <select id="findById" resultType="Chapter">
        SELECT * FROM chapter WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Chapter">
        SELECT * FROM chapter ORDER BY sort_order
    </select>

    <select id="findBySubject" resultType="Chapter">
        SELECT * FROM chapter WHERE subject = #{subject} ORDER BY sort_order
    </select>

    <insert id="insert" parameterType="Chapter" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chapter (subject, name, description, difficulty, sort_order)
        VALUES (#{subject}, #{name}, #{description}, #{difficulty}, #{sortOrder})
    </insert>

    <update id="update" parameterType="Chapter">
        UPDATE chapter
        <set>
            <if test="subject != null">subject = #{subject},</if>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="difficulty != null">difficulty = #{difficulty},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
